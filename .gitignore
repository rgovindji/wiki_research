# Personal
.DS_Store
.idea/
.vscode/

# Obsidian app state (per-user)
**/.obsidian/workspace.json
**/.obsidian/workspace-mobile.json

# Source material — copyrighted, not redistributed
raw/

# Local env / secrets
.env
.env.local
*.key
*.pem

# Build output / temp
node_modules/
*.log
.tmp/
n8n-credentials.env
*.env
.semiwiki-tmp/

# Python tooling (scripts/)
__pycache__/
*.pyc
.venv/
venv/

# SQLite FTS5 index built by scripts/wikidb.py
wiki.db
wiki.db-shm
wiki.db-wal

# AWS Lambda build artifacts
aws/build/

# gamma-desk is its own repo/project — never track it here (a gitlink without
# .gitmodules breaks every CI checkout; the nightly git add -A would re-add it)
gamma-desk/
